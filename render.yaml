services:
  - type: web
    name: GeodEdge
    env: python
    plan: free
    buildCommand: |
      npm install
      chmod +x ./node_modules/.bin/tailwindcss
      npm run build:css
      pip install -r requirements.txt
    startCommand: python app.py
